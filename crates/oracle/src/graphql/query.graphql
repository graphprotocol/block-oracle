query SubgraphState {
  networks {
    id
    removedAt {
      id
    }
    blockNumbers {
      id
      acceleration
      delta
      blockNumber
      epoch
    }
    nextArrayElement
    arrayIndex
    state
  }
}
